[package]
name = "rawshift-mediacodec-harness-native"
version = "0.0.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib"]

[target.'cfg(target_os = "android")'.dependencies]
jni = "0.22.4"
rawshift-hwdec = { path = "../../../crates/rawshift-hwdec", features = ["mediacodec"] }

[workspace]
