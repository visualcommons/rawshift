pub mod arw;
pub mod tiff;
// pub mod dng;

// pub trait RawDecoder {
//     /// Parse all metadata from a byte slice.
//     fn parse(&self, data: &[u8]) -> Result<RawImage, RawError>;

//     /// Decodes a raw image from a byte slice.
//     fn decode(&self, data: &[u8]) -> Result<RawImage, RawError>;
// }
